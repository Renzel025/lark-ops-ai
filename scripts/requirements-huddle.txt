# Minimal deps for scripts/slack_huddle_invite_all.py (Slack Huddle automation)
# Install on the server venv that runs this script, e.g.:
#   /path/to/.venv/bin/pip install -r scripts/requirements-huddle.txt
playwright>=1.40.0
playwright-stealth>=2.0.3
